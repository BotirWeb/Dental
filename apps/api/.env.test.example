# Integratsiya testlar (`pnpm test:integration`) uchun ALOHIDA baza.
# Bu faylni .env.test qilib nusxalang. HECH QACHON dev yoki prod
# DATABASE_URL bilan bir xil bo'lmasin — testlar ma'lumotni yozadi/o'chiradi,
# va DATABASE_URL nomida "test" so'zi bo'lmasa test ishga tushmaydi
# (src/testing/setupIntegration.ts qulfi).

DATABASE_URL=postgres://dental_app:CHANGE_ME@localhost:5432/dental_test
